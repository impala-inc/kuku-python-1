# xx_1_3
#
# 「1-2」の「kirakira_print」の「val」のように関数の内部で使用する値を実行時に受け取るための変数を「引数」と言います。
# 「a」と「b」の2つの引数をとって「a」から「b」を引いた値を返す「minus」を定義してください。

def plus(a, b):
    return a + b


result = plus(13, 22)
print(result)
